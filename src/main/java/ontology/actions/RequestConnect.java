package ontology.actions;

import jade.content.AgentAction;

/**
 * Peers ask Super Peers if they can connect. No fields because
 * the peer doesn't need to know anything about the requesting peer
 * in order to respond.
 */
public class RequestConnect implements AgentAction {

}
